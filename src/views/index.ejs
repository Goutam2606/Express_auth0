<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Express Auth0</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" />
</head>

<body class="bg-gray-200 flex flex-col items-center justify-center min-h-screen">
  <nav class="bg-white w-full p-4 shadow-md flex justify-between items-center fixed top-0 left-0 z-10">
    <% if (isAuthenticated) { %>
      <h1 class="text-3xl mb-2">
        <%= user.nickname %>
      </h1>
      <a href="/logout" class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600">Logout</a>
      <% } else { %>
        <h1 class="text-3xl mb-2">Express Auth0</h1>
        <a href="/login" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Login</a>
        <% } %>
  </nav>
  <div class="bg-white p-8 rounded-lg shadow-lg text-center mt-16 w-11/12 md:w-1/2 lg:w-1/3">
    <% if (isAuthenticated) { %>
      <img src="<%= user.picture %>" alt="Profile Picture" class="w-24 h-24 rounded-full mx-auto mb-4" />
      <h1 class="text-3xl mb-2">Hello, <%= user.name %>!</h1>
      <p class="text-gray-600 mb-4">
        <%= user.email %>
      </p>
      <p class="text-gray-600 mb-4">
        Last Login: <%= new Date(user.updated_at).toLocaleString() %>
      </p>
      <p class="text-gray-600 mb-4">
        Email Verified: <%= user.email_verified %>
      </p>
      <p class="text-gray-600 mb-4">First Name: <%= user.given_name %>
      </p>
      <p class="text-gray-600 mb-4">Last Name: <%= user.family_name %>
      </p>
      <p class="text-gray-600 mb-4">Full Name: <%= user.name %>
      </p>
      <p class="text-gray-600 mb-4">Gender: <%= user.gender %>
      </p>
      <p class="text-gray-600 mb-4">Birthdate: <%= user.birthdate %>
      </p>
      <p class="text-gray-600 mb-4">Phone Verified: <%= user.phone_number_verified %>
      </p>
      <p class="text-gray-600 mb-4">Phone Number: <%= user.phone_number %>
      </p>
      <p class="text-gray-600 mb-4">Time Zone: <%= user.zoneinfo %>
      </p>
      <% } else { %>
        <p class="text-lg mb-4">Login to Express Auth0 App</p>
        <a href="/login" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Login</a>
        <% } %>
  </div>
</body>

</html>